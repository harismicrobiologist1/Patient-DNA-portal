rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal Patient Directory and Health Vault
    match /patients/{dnaId} {
      allow read, write: if true;
    }
  }
}
